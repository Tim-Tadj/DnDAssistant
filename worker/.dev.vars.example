# Copy to `.dev.vars` for local `wrangler dev`. NOT committed.
# Use any 32+ character string locally; generate a real one for production
# with `wrangler secret put JWT_SECRET`.
JWT_SECRET="dev-secret-change-me-please-32-bytes-minimum"
