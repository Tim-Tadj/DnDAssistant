# Local dev: point the React app at the local Cloudflare Worker (wrangler dev).
# Was http://localhost:8081/api/v1 (the Java backend) during the migration.
REACT_APP_API_BASE=http://localhost:8787/api/v1
